dependencies:
  override:
    - pip install fabric
    - pip install requests
test:
  override:
    - cd .go_workspace/src/github.com/Bowery/broome/
    - make test
deployment:
  production:
    branch: master
    commands:
      - fab deploy
