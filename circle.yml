dependencies:
  override:
    - pip install fabric
    - pip install requests
test:
  override:
    - cd /home/ubuntu/.go_workspace/src/github.com/Bowery/broome/ && make test
deployment:
  production:
    branch: master
    commands:
      - fab deploy
